🎭 AI 审计天团阵容及专属指令（Prompt）
1号专家：MISRA-C 与安全合规性死磕专家
审计视角的侧重点： 静态扫描合规、内存安全风险、未定义行为（Undefined Behavior）。

你可以这样对AI说（Prompt）：

“你现在是国际顶尖的嵌入式 C 语言安全专家，精通 MISRA-C:2012 规范、ISO 26262 和 IEC 62304 医疗级认证标准。
请对我这300行 MIDI 传输层代码进行大锤敲击测试（Hammer Test）。特别审查：

是否存在任何隐式类型转换（如 size_t 与 uint8_t 的隐式截断风险）？

memcpy 之前的长度校验是否绝对没有溢出回绕（Wrap-around）的可能？

是否存在未初始化变量或野指针风险？

是否存在任何触发编译器未定义行为（UB）的潜在死角？
绝不要说漂亮话，请直接指出不合规的行号和修改方案。”

2号专家：极端硬件物理层与混沌通信专家
审计视角的侧重点： 丢包、错序、干扰、硬件溢出、极端边界状态。

你可以这样对AI说（Prompt）：

“你是一个在电磁干扰（EMI）环境极其恶劣的舞台上混迹了20年的 MIDI 通信专家。你最不信任物理层。
请审查这300行代码，并在大脑中模拟以下‘混沌物理场景’：

如果硬件拔插导致在 on_sysex_decoded 执行的中途，全局状态机被硬件中断（ISR）强行清空或改写，会发生什么？

假设上位机在没有发送 0x1 的情况下，以每毫秒1包的速度疯狂发送恶意的 0x2 到 0xF 中间包（Fuzzing 攻击），状态机的 _abort_tx 路径会不会被频繁触发导致 CPU 占满死锁？

基于 Windows MIDI API 和 TinyUSB 的底层 FIFO 机制，这300行 C 代码和 TS 代码是否存在时间片竞争（Race Condition）？”

3号专家：Web MIDI 生态与浏览器方言专家
审计视角的侧重点： 审查 TypeScript/JS 端的通信实现、事件循环、多设备并发。

你可以这样对AI说（Prompt）：

“你现在是 Web MIDI API 专家，精通 Chrome/Edge 的 Blink 底层 MIDI 驱动实现，以及 JavaScript 异步事件循环机制。
请严格审计这份上位机 TS/JS 代码：

await new Promise 引入的节流机制，在浏览器标签页进入后台（Background Tab）被系统强行降频挂起（Throttle）时，定时器精度失效会导致什么后果？分片会不会被拉长到设备超时？

传入的 jsonObj 如果包含 BigInt、undefined 或者带有不可靠的原型链属性，JSON.stringify 抛出异常时是否会拖垮前端业务主线程？是否有完整的 Try-Catch 防线？

当用户高频点击 UI 导致 sendJson 被并发调用（上一次发送尚未完成，下一次发送又进来了），你的类内部由于缺乏‘发送互斥锁’，是否会出现两个 JSON 的分片在 Web MIDI 发送队列里交叉混杂（Interleaving）的毁灭性灾难？如何加锁？”

4号专家：编译期优化与汇编级性能专家（针对 C 语言）
审计视角的侧重点： 内存开销、执行效率、-O3 激进优化下的安全性。

你可以这样对AI说（Prompt）：

“你现在是 GCC/Clang 编译器后端专家，擅长将 C 代码映射到 Cortex-M0+/M3/M4 等微控制器平台上。
请审查这 300 行 C 代码：

状态机中频繁使用的静态全局变量（如 recon_idx, in_multi_packet），在激进的 -O3 优化和硬件中断重入下，是否需要加上 volatile 关键字防止被编译器优化到寄存器中导致读取脏数据？

统一的 _abort_tx 使用了 goto 语句，编译器在生成汇编代码时，其跳转指令是否会导致流水线断流（Pipeline Flush）从而影响实时性？是否有更平坦（Flat）的优化写法？

从 RAM 开销和执行时间（Ticks）的角度，评估当前数据结构是否有进一步压榨的空间？”
