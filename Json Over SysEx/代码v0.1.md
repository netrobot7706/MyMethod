---


## 修复后的完整代码

### `sysex_codec.h`

```c
#ifndef SYSEX_CODEC_H
#define SYSEX_CODEC_H

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>

#ifdef __cplusplus
extern "C" {
#endif

#define SYSEX_MANUFACTURER_ID  0x7D
#define SYSEX_PROTOCOL_VERSION 0x1
#define SYSEX_MAX_PACKETS      15
#define SYSEX_MAX_PAYLOAD_PER_PACKET 256
#define SYSEX_HEADER_SIZE      6
#define SYSEX_FOOTER_SIZE      2

// 错误码
typedef enum {
    SYSEX_OK = 0,
    SYSEX_ERR_INVALID_FRAME = -1,
    SYSEX_ERR_WRONG_MANUFACTURER = -2,
    SYSEX_ERR_UNSUPPORTED_VERSION = -3,
    SYSEX_ERR_LENGTH_MISMATCH = -4,
    SYSEX_ERR_CHECKSUM_FAIL = -5,
    SYSEX_ERR_BUFFER_TOO_SMALL = -6,
    SYSEX_ERR_TOO_LARGE = -7,
    SYSEX_ERR_NULL_POINTER = -8,
    SYSEX_ERR_ILLEGAL_BYTE = -9
} sysex_error_t;

// 跨编译器弱符号宏
#if defined(__GNUC__) || defined(__clang__)
    #define SYSEX_WEAK __attribute__((weak))
#elif defined(__CC_ARM) || defined(__ARMCC_VERSION) || defined(__ICCARM__)
    #define SYSEX_WEAK __weak
#else
    #define SYSEX_WEAK
#endif

// 字节合法性检查（bit7 = 0）
static inline bool sysex_is_data_byte(uint8_t b) {
    return (b & 0x80u) == 0u;
}

#ifdef __cplusplus
}
#endif

#endif
```

---

### `sysex_encoder.h`

```c
#ifndef SYSEX_ENCODER_H
#define SYSEX_ENCODER_H

#include "sysex_codec.h"

typedef struct {
    const uint8_t *json_data;
    size_t         json_len;
    uint8_t        sub_id;
    uint8_t        total_packets;
    uint8_t        current_packet;
} sysex_encoder_t;

/**
 * 初始化编码器
 * @param ctx     编码器上下文
 * @param json    JSON 字符串（纯 ASCII，字节 ≤ 0x7F）
 * @param len     JSON 字节数（不含 '\0'）
 * @param sub_id  设备子标识 (0x00-0x7F)
 * @return SYSEX_OK 成功
 *         SYSEX_ERR_NULL_POINTER 指针为空
 *         SYSEX_ERR_TOO_LARGE 数据总长超出最大15包限制
 *         SYSEX_ERR_ILLEGAL_BYTE 输入含有非法字节（≥ 0x80）
 */
sysex_error_t sysex_encoder_init(sysex_encoder_t *ctx,
                                 const uint8_t *json, size_t len,
                                 uint8_t sub_id);

static inline uint8_t sysex_encoder_packet_count(const sysex_encoder_t *ctx) {
    return ctx->total_packets;
}

/**
 * 获取下一帧 SysEx 数据
 * @param ctx              编码器上下文
 * @param out_buf          输出缓冲区
 * @param out_buf_capacity 缓冲区容量（字节），至少 264
 * @param out_len          实际帧长度（包括 F0/F7）
 * @return true  成功生成一帧
 * @return false 无更多帧或容量不足
 */
bool sysex_encoder_next(sysex_encoder_t *ctx,
                        uint8_t *out_buf, size_t out_buf_capacity,
                        size_t *out_len);

#endif
```

---



---

### `sysex_decoder.h`

```c
#ifndef SYSEX_DECODER_H
#define SYSEX_DECODER_H

#include "sysex_codec.h"

#ifndef MAX_JSON_LEN
#error "MAX_JSON_LEN must be defined before including sysex_decoder.h"
#endif

typedef void (*sysex_json_handler_t)(const char *json, size_t len);
typedef void (*sysex_abort_handler_t)(void);

typedef struct {
    uint8_t  sub_id;
    uint8_t  expect_seg;
    size_t   recon_idx;
    bool     in_multi_packet;
    bool     initialized;                      // 初始化标记，防止未初始化访问
    char     recon_buf[MAX_JSON_LEN + 1];      // +1 for null terminator

    sysex_json_handler_t  json_handler;
    sysex_abort_handler_t abort_handler;
} sysex_decoder_t;

/**
 * 初始化解码器
 * @param decoder        解码器上下文
 * @param sub_id         期望的设备子标识
 * @param json_handler   重组完成回调（必须非 NULL）
 * @param abort_handler  事务中断回调（可为 NULL）
 * @return SYSEX_OK 成功，SYSEX_ERR_NULL_POINTER 如果 decoder 或 json_handler 为 NULL
 */
sysex_error_t sysex_decoder_init(sysex_decoder_t *decoder, uint8_t sub_id,
                                 sysex_json_handler_t json_handler,
                                 sysex_abort_handler_t abort_handler);

void sysex_decoder_process(sysex_decoder_t *decoder,
                           const uint8_t *data, size_t len);

void sysex_decoder_reset(sysex_decoder_t *decoder);

#endif
```

---

### `sysex_decoder.c`

```c
#include "sysex_decoder.h"
#include <string.h>

// 弱符号默认实现（用户可覆盖）
SYSEX_WEAK void sysex_tx_aborted_notify(void) {
}

/* 统一熔断出口：优先使用函数指针，无则回退弱符号 */
static void abort_tx(sysex_decoder_t *d) {
    if (d->abort_handler != NULL) {
        d->abort_handler();
    } else {
        sysex_tx_aborted_notify();
    }
    d->in_multi_packet = false;
    d->recon_idx = 0u;
    d->expect_seg = 0u;
}

sysex_error_t sysex_decoder_init(sysex_decoder_t *decoder, uint8_t sub_id,
                                 sysex_json_handler_t json_handler,
                                 sysex_abort_handler_t abort_handler) {
    if (decoder == NULL) {
        return SYSEX_ERR_NULL_POINTER;
    }
    if (json_handler == NULL) {
        return SYSEX_ERR_NULL_POINTER;
    }

    decoder->sub_id = sub_id & 0x7Fu;
    decoder->json_handler = json_handler;
    decoder->abort_handler = abort_handler;
    decoder->expect_seg = 0u;
    decoder->recon_idx = 0u;
    decoder->in_multi_packet = false;
    decoder->initialized = true;                         // 设置初始化标记
    (void)memset(decoder->recon_buf, 0, sizeof(decoder->recon_buf));
    return SYSEX_OK;
}

void sysex_decoder_reset(sysex_decoder_t *decoder) {
    if (decoder == NULL || !decoder->initialized) {
        return;   // 未初始化，安全返回
    }
    // 仅在已初始化且正在进行多包事务时才触发 abort 通知
    if (decoder->in_multi_packet) {
        abort_tx(decoder);
    } else {
        decoder->recon_idx = 0u;
        decoder->expect_seg = 0u;
    }
}

void sysex_decoder_process(sysex_decoder_t *decoder,
                           const uint8_t *data, size_t len) {
    if (decoder == NULL || !decoder->initialized || data == NULL) {
        return;
    }

    if (len < 2u || data[0] != 0xF0u || data[len-1u] != 0xF7u) {
        return;
    }

    const uint8_t *inner = &data[1];
    size_t inner_len = len - 2u;

    if (inner_len < 5u) {
        return;
    }

    // 1. 厂商 ID
    if (inner[0] != SYSEX_MANUFACTURER_ID) {
        return;
    }

    // 2. Sub-ID (必须为 7-bit 且匹配)
    uint8_t sub_id = inner[1];
    if (!sysex_is_data_byte(sub_id) || sub_id != decoder->sub_id) {
        return;
    }

    // 3. Ver/Seg
    uint8_t ver_seg = inner[2];
    if (!sysex_is_data_byte(ver_seg)) {
        return;
    }
    uint8_t version = (uint8_t)((ver_seg >> 4) & 0x0Fu);
    if (version != SYSEX_PROTOCOL_VERSION) {
        return;
    }
    uint8_t segment = ver_seg & 0x0Fu;

    // 4. 长度字段 (必须为 7-bit)
    uint8_t len_high = inner[3];
    uint8_t len_low  = inner[4];
    if (!sysex_is_data_byte(len_high) || !sysex_is_data_byte(len_low)) {
        return;
    }
    size_t declared_len = ((size_t)len_high << 7) | (size_t)len_low;

    // 检查帧内是否包含完整 payload + XOR (用减法防止回绕)
    // inner_len >= 5 已保证减法不溢出
    if (declared_len + 1u > inner_len - 5u) {
        return;
    }

    const uint8_t *payload = &inner[5];
    size_t payload_len = declared_len;

    // 检查 payload 全部为 7-bit
    for (size_t i = 0; i < payload_len; i++) {
        if (!sysex_is_data_byte(payload[i])) {
            return;
        }
    }

    // 5. XOR 校验
    uint8_t xor_calc = sub_id ^ ver_seg ^ len_high ^ len_low;
    for (size_t i = 0; i < payload_len; i++) {
        xor_calc ^= payload[i];
    }
    xor_calc &= 0x7Fu;
    uint8_t xor_recv = inner[5u + payload_len];
    if (!sysex_is_data_byte(xor_recv) || xor_calc != xor_recv) {
        return;
    }

    // 6. 单包超长检查
    if (payload_len > (size_t)MAX_JSON_LEN) {
        abort_tx(decoder);
        return;
    }

    // ━━━ 状态机处理 ━━━
    if (segment == 0x1u) {
        // 首包
        if (decoder->in_multi_packet) {
            abort_tx(decoder);
        }
        decoder->recon_idx = 0u;
        decoder->expect_seg = 0x2u;
        decoder->in_multi_packet = true;
        (void)memcpy(decoder->recon_buf, payload, payload_len);
        decoder->recon_idx = payload_len;
    }
    else if (segment == 0x0u) {
        // 末包 / 单包
        if (decoder->in_multi_packet) {
            // 用减法保证 recon_idx + payload_len 不溢出
            if (payload_len > ((size_t)MAX_JSON_LEN - decoder->recon_idx)) {
                abort_tx(decoder);
                return;
            }
            (void)memcpy(&decoder->recon_buf[decoder->recon_idx], payload, payload_len);
            decoder->recon_idx += payload_len;
            decoder->recon_buf[decoder->recon_idx] = '\0';
            decoder->json_handler(decoder->recon_buf, decoder->recon_idx);
        }
        else if (decoder->recon_idx == 0u) {
            // 纯单包
            (void)memcpy(decoder->recon_buf, payload, payload_len);
            decoder->recon_buf[payload_len] = '\0';
            decoder->json_handler(decoder->recon_buf, payload_len);
        }
        else {
            // 孤立末包
            abort_tx(decoder);
            return;
        }

        // 事务正常结束，复位
        decoder->recon_idx = 0u;
        decoder->expect_seg = 0u;
        decoder->in_multi_packet = false;
    }
    else {
        // 中间包 (0x2 ~ 0xF)
        if (decoder->in_multi_packet &&
            decoder->expect_seg <= 0x0Fu &&          // 上限保护
            segment == decoder->expect_seg &&
            (payload_len <= ((size_t)MAX_JSON_LEN - decoder->recon_idx)))
        {
            (void)memcpy(&decoder->recon_buf[decoder->recon_idx], payload, payload_len);
            decoder->recon_idx += payload_len;
            decoder->expect_seg++;
        }
        else {
            abort_tx(decoder);
        }
    }
}
```


---

## 修改后的 `sysex_decoder.c`（完整文件）

```c
#include "sysex_decoder.h"
#include <string.h>

// 弱符号默认实现（用户可覆盖）
SYSEX_WEAK void sysex_tx_aborted_notify(void) {
}

/* 统一熔断出口：优先使用函数指针，无则回退弱符号 */
static void abort_tx(sysex_decoder_t *d) {
    if (d->abort_handler != NULL) {
        d->abort_handler();
    } else {
        sysex_tx_aborted_notify();
    }
    d->in_multi_packet = false;
    d->recon_idx = 0u;
    d->expect_seg = 0u;
}

sysex_error_t sysex_decoder_init(sysex_decoder_t *decoder, uint8_t sub_id,
                                 sysex_json_handler_t json_handler,
                                 sysex_abort_handler_t abort_handler) {
    if (decoder == NULL) {
        return SYSEX_ERR_NULL_POINTER;
    }
    if (json_handler == NULL) {
        return SYSEX_ERR_NULL_POINTER;
    }

    decoder->sub_id = sub_id & 0x7Fu;
    decoder->json_handler = json_handler;
    decoder->abort_handler = abort_handler;
    decoder->expect_seg = 0u;
    decoder->recon_idx = 0u;
    decoder->in_multi_packet = false;
    decoder->initialized = true;
    (void)memset(decoder->recon_buf, 0, sizeof(decoder->recon_buf));
    return SYSEX_OK;
}

void sysex_decoder_reset(sysex_decoder_t *decoder) {
    if (decoder == NULL || !decoder->initialized) {
        return;
    }
    if (decoder->in_multi_packet) {
        abort_tx(decoder);
    } else {
        decoder->recon_idx = 0u;
        decoder->expect_seg = 0u;
    }
}

void sysex_decoder_process(sysex_decoder_t *decoder,
                           const uint8_t *data, size_t len) {
    if (decoder == NULL || !decoder->initialized || data == NULL) {
        return;
    }

    if (len < 2u || data[0] != 0xF0u || data[len-1u] != 0xF7u) {
        return;
    }

    const uint8_t *inner = &data[1];
    size_t inner_len = len - 2u;

    // 最小头部 + XOR：6 字节
    if (inner_len < 6u) {
        return;
    }

    // 1. 厂商 ID
    if (inner[0] != SYSEX_MANUFACTURER_ID) {
        return;
    }

    // 2. Sub-ID
    uint8_t sub_id = inner[1];
    if (!sysex_is_data_byte(sub_id) || sub_id != decoder->sub_id) {
        return;
    }

    // 3. Ver/Seg
    uint8_t ver_seg = inner[2];
    if (!sysex_is_data_byte(ver_seg)) {
        return;
    }
    uint8_t version = (uint8_t)((ver_seg >> 4) & 0x0Fu);
    if (version != SYSEX_PROTOCOL_VERSION) {
        return;
    }
    uint8_t segment = ver_seg & 0x0Fu;

    // 4. 长度字段
    uint8_t len_high = inner[3];
    uint8_t len_low  = inner[4];
    if (!sysex_is_data_byte(len_high) || !sysex_is_data_byte(len_low)) {
        return;
    }
    size_t declared_len = ((size_t)len_high << 7) | (size_t)len_low;

    // 帧完整性检查（用减法防止回绕）
    if (declared_len + 1u > inner_len - 5u) {
        return;
    }

    const uint8_t *payload = &inner[5];
    size_t payload_len = declared_len;

    // 检查 payload 全部为 7-bit
    for (size_t i = 0; i < payload_len; i++) {
        if (!sysex_is_data_byte(payload[i])) {
            return;
        }
    }

    // 5. XOR 校验
    uint8_t xor_calc = sub_id ^ ver_seg ^ len_high ^ len_low;
    for (size_t i = 0; i < payload_len; i++) {
        xor_calc ^= payload[i];
    }
    xor_calc &= 0x7Fu;
    uint8_t xor_recv = inner[5u + payload_len];
    if (!sysex_is_data_byte(xor_recv) || xor_calc != xor_recv) {
        return;
    }

    // 6. 单包超长检查（修复：仅多包事务才触发 abort）
    if (payload_len > (size_t)MAX_JSON_LEN) {
        if (decoder->in_multi_packet) {
            abort_tx(decoder);
        }
        return;
    }

    // ━━━ 状态机处理 ━━━
    if (segment == 0x1u) {
        // 首包
        if (decoder->in_multi_packet) {
            abort_tx(decoder);
        }
        decoder->recon_idx = 0u;
        decoder->expect_seg = 0x2u;
        decoder->in_multi_packet = true;
        (void)memcpy(decoder->recon_buf, payload, payload_len);
        decoder->recon_idx = payload_len;
    }
    else if (segment == 0x0u) {
        // 末包 / 单包
        if (decoder->in_multi_packet) {
            // 防御：多包事务中末包到来时，expect_seg 至少应为 0x02
            if (decoder->expect_seg < 0x02u) {
                abort_tx(decoder);
                return;
            }

            if (payload_len > ((size_t)MAX_JSON_LEN - decoder->recon_idx)) {
                abort_tx(decoder);
                return;
            }
            (void)memcpy(&decoder->recon_buf[decoder->recon_idx], payload, payload_len);
            decoder->recon_idx += payload_len;
            decoder->recon_buf[decoder->recon_idx] = '\0';
            decoder->json_handler(decoder->recon_buf, decoder->recon_idx);
        }
        else if (decoder->recon_idx == 0u) {
            // 纯单包
            (void)memcpy(decoder->recon_buf, payload, payload_len);
            decoder->recon_buf[payload_len] = '\0';
            decoder->json_handler(decoder->recon_buf, payload_len);
        }
        else {
            // 孤立末包
            abort_tx(decoder);
            return;
        }

        // 事务正常结束，复位
        decoder->recon_idx = 0u;
        decoder->expect_seg = 0u;
        decoder->in_multi_packet = false;
    }
    else {
        // 中间包 (0x2 ~ 0xF)
        if (decoder->in_multi_packet &&
            decoder->expect_seg <= 0x0Fu &&
            segment == decoder->expect_seg &&
            (payload_len <= ((size_t)MAX_JSON_LEN - decoder->recon_idx)))
        {
            (void)memcpy(&decoder->recon_buf[decoder->recon_idx], payload, payload_len);
            decoder->recon_idx += payload_len;
            decoder->expect_seg++;
        }
        else {
            abort_tx(decoder);
        }
    }
}
```
