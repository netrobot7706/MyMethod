/**
 * @file    pot_app.h
 * @brief   电位器管线 - 应用层内部接口
 *
 * 本文件仅供管线内部使用，不对外暴露。
 */

#ifndef POT_APP_H
#define POT_APP_H

#include "market/pot_api.h"  // 公开 API 类型和常量

// 内部函数（如有需要可在此声明，当前无额外内部接口）

#endif // POT_APP_H
